import { PreviusStepDirective } from './previus-step.directive';

describe('PreviusStepDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviusStepDirective();
    expect(directive).toBeTruthy();
  });
});
