import { NextStepDirective } from './next-step.directive';

describe('NextStepDirective', () => {
  it('should create an instance', () => {
    const directive = new NextStepDirective();
    expect(directive).toBeTruthy();
  });
});
