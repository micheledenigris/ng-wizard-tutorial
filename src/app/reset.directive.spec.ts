import { ResetDirective } from './reset.directive';

describe('ResetDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetDirective();
    expect(directive).toBeTruthy();
  });
});
