import { FocusOnStepsDirective } from './focus-on-step.directive';

describe('EnableFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnStepsDirective();
    expect(directive).toBeTruthy();
  });
});
